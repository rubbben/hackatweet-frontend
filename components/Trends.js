import styles from "../styles/Trends.module.css";

function Trends() {

    // Récupérer les données des hashtags depuis le backend
    // importer le composante Hashtag
    // Faire un map des données pour les transformer en tableau JSX de composants Hashtags
    let hashtags = []

    return (
        <div>
            {hashtags}
        </div>
    );
}

export default Trends;
